# ConnectPad 项目记录

## 目标

提供精简的 Windows 控制台，复用 scrcpy 4.1 完成 Android 11+ 的 USB / Wi-Fi 低延迟投屏与键鼠控制。

## 固定决策

- .NET 8 WPF，Windows x64 便携发布。
- 无 Android APK、无账号、无互联网串流。
- scrcpy 4.1 + ADB；H.264、60 FPS、最长边 1920、无音频。
- UI 只保留连接所需控件和简短状态。

## 进度

| 阶段 | 状态 | 证据 |
|---|---|---|
| 1. 基础工程与依赖 | 已完成 | Release 构建通过；scrcpy 4.1 哈希匹配官方值 |
| 2. 连接控制台 | 已完成 | 设备刷新、配对、连接与状态处理编译通过 |
| 3. 串流集成 | 已完成 | scrcpy 4.1 参数、自检与版本验证通过 |
| 4. 实机测试 | USB 已通过；Wi-Fi 待测 | 荣耀 ROD-W09 USB 投屏与 15 分钟稳定性通过 |
| 5. 便携发布 | 已完成 | 自包含 ZIP、无 PATH 自检与视觉检查通过 |

## 阶段记录

### 2026-07-18 — 项目启动

- 创建最小 WPF 工程、README 与本项目记录。
- 等待基础构建验证与 scrcpy 依赖落位。

### 2026-07-18 — 阶段 1：基础工程与依赖完成

- `dotnet build -c Release`：0 警告、0 错误。
- 下载官方 `scrcpy-win64-v4.1.zip` 并解压到 `tools/scrcpy`。
- SHA-256：`5B12172B3264B2889F4583EE64752CE832E29BC8B1089DCA81093459697165DB`，与官方值一致。

### 2026-07-18 — 阶段 2：连接控制台完成

- 完成精简 UI：设备、刷新、启动投屏、无线配对、无线连接、单行状态。
- 完成 `adb devices -l` 解析及未授权、离线、多设备状态显示。
- 配对码仅通过标准输入传递；所有参数使用 `ArgumentList`，不经过 Shell。
- Release 构建：0 警告、0 错误。

### 2026-07-18 — 阶段 3：串流集成完成

- 启动参数固定为 H.264、60 FPS、最长边 1920、无音频、保持唤醒。
- scrcpy 在独立原生窗口运行；关闭或异常退出会回写单行状态。
- `--self-test` 覆盖设备解析、状态、地址、配对码和串流参数，退出码为 0。
- 捆绑版本验证：ADB 37.0.0、scrcpy 4.1。
- Release 构建：0 警告、0 错误。

### 2026-07-18 — 阶段 4：实机测试等待设备

- `adb devices -l` 可正常启动 ADB 服务，但目前未发现已连接设备。
- USB、无线、键鼠、帧率和 15 分钟稳定性仍需真实 Android 11+ 设备验收。

### 2026-07-18 — 阶段 4A：USB 实机测试通过

- 设备：荣耀平板 V8 Pro 12.1，Android 型号 `ROD-W09`。
- Windows 已正确加载 Microsoft WinUSB 驱动；该机型无法被 ADB 原生后端枚举，切换 `ADB_LIBUSB=1` 后稳定识别。
- 已将 libusb 后端写入 ADB 与 scrcpy 子进程环境，重启 ADB 后 ConnectPad 可自行发现设备。
- USB 授权、设备选择和真实画面启动通过，首帧低于 3 秒。
- 连续 15 分钟：scrcpy 始终响应、USB 始终为 `device`，内存范围约 81.4–95.5 MB，无持续增长或断流。
- 无线配对/串流仍待验收。

### 2026-07-18 — 阶段 5：便携发布完成

- 生成 `ConnectPad-win-x64-v0.1.0.zip`，包含自包含程序、scrcpy/ADB、使用说明与许可文件。
- 在全新目录、清空 PATH 后运行 `--self-test`，退出码为 0。
- 解压包内版本验证：ADB 37.0.0、scrcpy 4.1。
- 修复荣耀平板 USB 枚举后重新发布；最新 SHA-256：`1D07B73527CE5BCDCFAE74E9E6187D6B35ED607A06EE249FD216A0C69886D999`。
- 本地视觉检查通过；界面仅显示设备、刷新、启动、无线配对/连接和单行状态，底部无裁切。
